spring:
  profiles:
    active: staging
  main:
    banner-mode: off
logging:
  level:
    root: INFO
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG

